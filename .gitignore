# Claude Code local settings
.claude/

# macOS
.DS_Store

# Node / misc (defensive — not used at runtime)
node_modules/

# Local test fixtures (real books used for smoke testing, never committed)
_test/

# Note: vendor/ is intentionally tracked. Books vendors foliate-js and
# pdfjs-dist at pinned versions instead of CDN-loading at runtime.
